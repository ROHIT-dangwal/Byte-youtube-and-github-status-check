<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Byte auth</title>
    <link rel="icon" href="./BYTELOGO.png" type="image/x-icon" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Sofadi+One&display=swap"
      rel="stylesheet"
    />

    <style>
      body {
        font-family: "Sofadi One", system-ui;
        background-color: black;
        color: white;
        overflow: hidden;
      }
      .main {
        width: 100vw;
        height: 100vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
      }
      img {
        width: 200px;
        height: 200px;
        border-radius: 50%;
      }

      a {
        background-color: rgb(238, 228, 228);
        position: relative;
        border-radius: 12px;
        padding: 10px;
        text-decoration: none;
        color: rgb(17, 16, 16);
        display: flex;
        overflow: hidden;
        margin-top: 40px;
        box-shadow: 6px 7px 15px 3px #575753;
      }

      a::after {
        content: "";
        position: absolute;
        top: 0%;
        left: -100%;
        width: 100%;
        height: 100%;
        background-color: rgba(12, 13, 12, 0.566);
      }
      a:hover::after {
        top: 0%;
        left: 100%;
        transition: all ease-out 1s;
      }
      a:hover {
        transform: scale(1.2);
      }
    </style>
  </head>
  <body>
    <div class="main">
      <h1>You Are Subscribed to BYTE !</h1>

      <img src="<%=user.photos[0].value%>" placeholder="Profile" />
      <h1><%= user.displayName %></h1>
      <a href="logout">Logout</a>
    </div>
  </body>
</html>
